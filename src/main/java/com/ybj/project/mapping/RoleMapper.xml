<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ybj.project.dao.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ybj.project.model.Role">
        <id column="ID" property="id" />
        <result column="NAME" property="name" />
        <result column="STATUS" property="status" />
        <result column="CREATOR" property="creator" />
        <result column="CREATOR_NAME" property="creatorName" />
        <result column="CREATE_TIME" property="createTime" />
        <result column="UPDATOR" property="updator" />
        <result column="UPDATOR_NAME" property="updatorName" />
        <result column="UPDATE_TIME" property="updateTime" />
        <result column="REMARK" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ID, NAME, STATUS, CREATOR, CREATOR_NAME, CREATE_TIME, UPDATOR, UPDATOR_NAME, UPDATE_TIME, REMARK
    </sql>


    <select id="selectRoleListByUsername" resultMap="BaseResultMap">
  SELECT
     	SR.*
        FROM
            sys_user SU
        JOIN sys_user_role SUR ON SU.ID = SUR.USER_ID
        JOIN sys_role SR ON SUR.ROLE_ID = SR.ID
        WHERE USERNAME = #{username} and SR.status = '0'
    </select>

</mapper>
